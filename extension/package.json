{
  "name": "flowfixer",
  "displayName": "FlowFixer",
  "description": "AI Bug Tutor for VS Code — turns every error into a lesson",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.85.0"
  },
  "main": "./out/extension.js",
  "scripts": {
    "build": "node esbuild.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^1.0.0"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "esbuild": "^0.24.0"
  }
}
